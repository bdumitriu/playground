#!/usr/bin/perl

# Author: Bogdan DUMITRIU
# Date: 07.09.2000
# Function: reads and prints an array.
# Is working: yes
# Yet to be done: all done

print("The number of elements: ");
$n = <STDIN>;
chop($n);
$i = 0;
while ($i < $n)
{
    print("Element nr. ",  $i+1, ": ");
    $a[$i] = <STDIN>;
    chop($a[$i++]);
}
$i = 0;
print("The elements of the array: ");
while ($i < $n)
{
    print($a[$i++], " ");
}
print("\n");