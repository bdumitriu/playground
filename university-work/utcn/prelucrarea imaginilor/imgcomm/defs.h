#ifndef DEFS_H
#define DEFS_H

/**
 * Bitmap types (8 bit, 24 bit). Bit8 represents an 8-bit
 * encoded bitmap, while Bit24 represents a 24-bit encoded
 * bitmap.
 */
enum BitmapType {Bit8, Bit24};

/**
 * Definition of the byte type.
 */
typedef unsigned char byte;

#endif // DEFS_H
