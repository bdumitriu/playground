#!/usr/bin/perl

# Author: Bogdan DUMITRIU
# Date: 15.08.2000
# Function: reads two lines from standard input and writes them back.
# Is working: yes
# Yet to be done: all done

print("First line: ");
$inputline1 = <STDIN>;
print("Second line: ");
$inputline2 = <STDIN>;
print($inputline1, $inputline2);
